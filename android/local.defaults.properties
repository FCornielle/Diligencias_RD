# Valores por defecto para el build cuando local.properties no define secretos.
# PLACEHOLDER_API_KEY activa el modo demo local sin cuenta GCP.
# Si se habilita Google en el futuro, la clave real va en android/local.properties.
MAPS_API_KEY=PLACEHOLDER_API_KEY
