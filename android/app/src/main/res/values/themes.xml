<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.DiligenciaRD" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- AppCompat requerido por NavigationActivity (Navigation SDK) -->
    <style name="Theme.DiligenciaRD.Navigation" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
